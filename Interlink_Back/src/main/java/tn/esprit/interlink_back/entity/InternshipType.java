package tn.esprit.interlink_back.entity;

public enum InternshipType {
    FULL_TIME, // Stage à plein temps
    PART_TIME, // Stage à temps partiel
    REMOTE,    // Stage à distance
    HYBRID     // Stage hybride (présentiel + télétravail)
}
